Check the current state of the interface design system:

1. Read `.interface-design/system.md` if it exists
2. Report: direction, depth strategy, spacing base, documented patterns
3. If no system.md exists, report that and suggest running `/interface-design-init`
